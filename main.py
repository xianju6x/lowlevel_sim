state = None

HIGH = 0
FALL = 1
DOWN = 2
RAISE = 3

data_bus = []
right_bus = []
left_bus = []
addr_bus = []


class HS7474(object):
    signal = 0
    din = 0
    dout 0

    def latch(self,signal,din):
        if signal = RAISE:
            self.dout = din


def main():
    while True:
        if interrupt:
            microcode = microm=[interrupt_no]
        else:
            opcode = RAM[pc++]
            microcode = microm[opcode]
            regA_l =  microcode[regA_l_index]
            regA_en = microcode[regA_en_index]
             
            
if __name__ == '__main__':
    main()
